using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WeaponType              // 무기 종류 enum
{
    Sword =0,
    Dagger=1,
    Bow=2
}

public enum WeaponSelectSword       // 검 enum
{
    SwordA=0,
    SwordB,
    SwordC,
    SwordD,
    SwordE
}public enum WeaponSelectDagger     // 단검 enum
{
    DaggerA=0,
    DaggerB,
    DaggerC,
    DaggerD,
    DaggerE
}public enum WeaponSelectBow        // 활 enum
{
    BowA=0,
    BowB,
    BowC,
    BowD,
    BowE
}
